interface:
  display_name: "A3S Test"
  short_description: "Test Web, GUI, and TUI workflows with evidence"
  default_prompt: "Use $a3s-test to explore this application, capture bounded evidence, and promote the stable workflow to typed regression coverage."
