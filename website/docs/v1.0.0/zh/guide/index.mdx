---
title: 跑通第一个 Web 测试
description: 用持久 Agent 会话观察当前页面，执行一个受限动作并保存证据，再把跑通的路径固化为 ACL。
---

# 跑通第一个 Web 测试

这份指南从一个正在运行的本地页面开始。完成后，你会得到一份可复查的会话记录，其中包含目标、每次观察、实际动作、截图和终态报告。路径稳定后，可以继续把它写成 ACL 回归套件。

## 先选对入口

| 你现在要完成的事              | 从这里开始                                             |
| ----------------------------- | ------------------------------------------------------ |
| 探索陌生页面或复现一个问题    | 继续本页，使用持久 Agent 会话                          |
| 重复运行已经明确的流程        | [探索与回归工作流](./workflows.mdx)                    |
| 查询每个动作、等待和证据产物  | [动作、等待与证据](./actions-and-evidence.mdx)         |
| 编写控件、布局和稳定性断言    | [断言与稳定性](./assertions.mdx)                       |
| 选择 Web、GUI、TUI 或 MCP     | [Web、GUI、TUI 与 MCP](./surfaces-and-mcp.mdx)         |
| 让页面提供组件、定位器和坐标  | [接入 Web Test Kit](./testkit.mdx)                     |
| 查清页面上下文字段与失效规则  | [Page Context 详解](../concepts/page-context.mdx)      |
| 点选问题并交给 Agent 修复     | [人工评审与自动修复](./repairs.mdx)                    |
| 对照 PRD、设计稿与真实页面    | [生成 Surface Contract](./contracts.mdx)               |
| 接入 LLM、视觉定位或设计模型  | [LLM、视觉定位与设计 Provider](./providers.mdx)        |
| 把确定性套件分片到远程 Worker | [Worker 与分布式执行](./distributed.mdx)               |
| 核对模型、人工和源码权限      | [权限与安全模型](../concepts/authority-and-safety.mdx) |
| 按入口核对所有当前能力        | [完整能力参考](../reference/capabilities.mdx)          |
| 根据错误码和证据排查失败      | [故障排查](./troubleshooting.mdx)                      |

下面的示例假设产品已经运行在 `http://127.0.0.1:3000/checkout`，并且确认页有一个可观察的标题。请把 URL、目标和成功条件换成当前项目的真实状态。

## 安装 CLI 与 Agent Skill

macOS 或 Linux

```bash
curl -fsSL https://github.com/A3S-Lab/Test/releases/latest/download/install.sh |
  sh -s -- --version v1.0.0
```

Windows PowerShell

```powershell
& ([scriptblock]::Create((irm 'https://github.com/A3S-Lab/Test/releases/latest/download/install.ps1'))) -Version v1.0.0
```

安装器会下载当前平台的 CLI，校验 SHA-256，并为检测到的编码 Agent 安装同版本 Skill。需要固定版本、Agent 或目标目录时，请查看[安装指南](./installation.mdx)。

## 1. 启动持久会话

先启动本地产品，再声明测试目标和可以从页面确认的成功条件。

```bash
a3s-test agent start http://127.0.0.1:3000/checkout \
  --session checkout \
  --goal "Complete checkout with the fixture account" \
  --success "The confirmation heading is visible" \
  --json
```

会话会保留浏览器、策略边界和追加式事件记录，直到显式完成、取消或超时。

## 2. 读取最新观察

```bash
a3s-test agent observe --session checkout --interactive --json
```

响应会带回新的 `observation_id` 和当前可操作语义引用。

```text
observation_id: 1
@e1 [button] Continue
```

同一次观察可能出现三种引用。它们的权限不同。

| 引用  | 来自哪里                   | 能否作为动作目标                             |
| ----- | -------------------------- | -------------------------------------------- |
| `@eN` | 浏览器可访问语义           | 可以，但只在当前观察中有效                   |
| `@cN` | Test Kit Page Context      | 可以，但只代表可唯一操作且属于当前观察的节点 |
| `@uN` | Test Kit UI 理解的补充证据 | 不可以，只用于关联样式、布局、状态和动效证据 |

`@uN` 是只读证据引用。不要把它写进 `click`、`fill`、`drag` 或 ACL 动作；A3S Test 会在准入或驱动执行前拒绝它。页面变化后，`@eN` 与 `@cN` 也会失效，继续操作前必须重新观察。

## 3. 执行一个受限动作

把动作绑定到刚才的观察。

```bash
a3s-test agent click @e1 \
  --session checkout \
  --observation 1 \
  --json
```

A3S Test 会在驱动执行前检查动作 Schema、会话能力、策略和观察修订。页面状态一旦变化，旧引用立即失效。继续操作前必须再次运行 `agent observe`。

## 4. 保存证据并结束会话

确认目标状态已经出现后，保存截图并写入终态报告。

```bash
a3s-test agent screenshot screenshots/confirmation.png \
  --session checkout \
  --json

a3s-test agent finish \
  --session checkout \
  --status passed \
  --summary "Checkout completed and confirmation was observed" \
  --json
```

每个工作区保留追加式记录。

```text
.a3s-test/agent-sessions/checkout/
├── session.json
├── events.jsonl
├── report.json
└── artifacts/
    └── screenshots/confirmation.png
```

`events.jsonl` 说明每一步发生了什么，`report.json` 保存终态与摘要，`artifacts/` 只包含这次会话拥有的证据文件。

## 5. 把稳定路径写成回归

Agent 会话适合发现路径。动作、等待和成功条件已经明确后，把它们写成 ACL，再用 `a3s-test check` 做静态准入，用 `a3s-test run` 在本地或 CI 重复执行。完整示例见[探索与回归工作流](./workflows.mdx)。
