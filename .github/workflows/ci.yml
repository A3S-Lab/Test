name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release-metadata:
    name: Release metadata
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Test release metadata admission
        run: node --test tests/release-metadata.test.mjs

      - name: Verify current release metadata
        run: node scripts/check-release-metadata.mjs

  runner-image:
    name: Hermetic runner image
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and smoke-test the restricted runner
        env:
          A3S_TEST_RUNNER_CACHE_DIR: ${{ runner.temp }}/a3s-test-runner-cache
          A3S_TEST_RUNNER_IMAGE_VERSION: ci
          A3S_TEST_RUNNER_SOURCE_REVISION: ${{ github.sha }}
        run: sh images/runner/test-image.sh a3s-test-runner:ci

  testkit:
    name: Test Kit quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/testkit

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: packages/testkit/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check types
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Build package
        run: npm run build

      - name: Verify package consumers
        run: npm run check:package

  quality:
    name: Rust quality (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-14
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Rust 1.85
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run tests
        shell: bash
        run: >-
          bash .github/scripts/run-cargo-test.sh
          cargo test --workspace --all-targets --locked

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Assert Unix process-test cleanup
        if: always() && runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          survivors="$(
            ps -axo pid=,command= | awk '
              /a3s-test-(browser|cua|tui)-watchdog/ && $0 !~ /awk/ { print }
              /successful_command_descendant_fixture/ && $0 !~ /awk/ { print }
              /owned_process_tree_descendant_fixture/ && $0 !~ /awk/ { print }
              /a3s-test-tui-(descendant|interrupt|second-interrupt|host-crash)-fixture/ && $0 !~ /awk/ { print }
              /a3s-test .*worker serve/ && $0 !~ /awk/ { print }
              /fake-agent-browser/ && $0 !~ /awk/ { print }
            '
          )"
          runtime_roots=(/tmp)
          if [[ -n "${TMPDIR:-}" && "$TMPDIR" != /tmp ]]; then
            runtime_roots+=("$TMPDIR")
          fi
          runtimes="$(
            for runtime_root in "${runtime_roots[@]}"; do
              find "$runtime_root" -maxdepth 1 -type d \
                \( -name 'a3st-*' -o -name 'a3s-test-browser-*' \) -print
            done
          )"
          if [[ -n "$survivors" || -n "$runtimes" ]]; then
            printf 'Surviving process-test helpers:\n%s\n' "$survivors" >&2
            printf 'Surviving private runtimes:\n%s\n' "$runtimes" >&2
            exit 1
          fi

      - name: Assert Windows process-test cleanup
        if: always() && runner.os == 'Windows'
        shell: pwsh
        run: |
          $survivors = @(
            Get-CimInstance Win32_Process | Where-Object {
              $_.CommandLine -match 'successful_command_descendant_fixture' -or
              $_.CommandLine -match 'owned_process_tree_descendant_fixture' -or
              $_.CommandLine -match 'a3s-test-tui-windows-descendant-fixture' -or
              $_.CommandLine -match 'a3s-test(\.exe)? worker serve'
            }
          )
          $runtimeRoot = [System.IO.Path]::GetTempPath()
          $runtimes = @(
            Get-ChildItem -LiteralPath $runtimeRoot -Directory -ErrorAction SilentlyContinue |
              Where-Object {
                $_.Name -like 'a3st-*' -or
                $_.Name -like 'a3s-test-browser-*'
              }
          )
          if ($survivors.Count -gt 0 -or $runtimes.Count -gt 0) {
            $survivors |
              Select-Object ProcessId, ParentProcessId, Name, CommandLine |
              Format-List | Out-String | Write-Error
            $runtimes | Select-Object FullName | Format-List | Out-String | Write-Error
            exit 1
          }

  web-e2e:
    name: Hermetic Web E2E (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-14
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Rust 1.85
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            packages/testkit/package-lock.json
            website/package-lock.json

      - name: Install Test Kit dependencies
        run: npm ci --prefix packages/testkit

      - name: Install website dependencies
        run: npm ci --prefix website

      - name: Install the admitted standalone browser
        run: |
          npm install --global agent-browser@0.26.0
          agent-browser --version

      - name: Install pinned Chrome Headless Shell
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          version=147.0.7727.117
          case "$(uname -m)" in
            arm64) platform=mac-arm64 ;;
            x86_64) platform=mac-x64 ;;
            *) printf 'Unsupported macOS architecture: %s\n' "$(uname -m)" >&2; exit 1 ;;
          esac
          archive="$RUNNER_TEMP/chrome-headless-shell.zip"
          root="$RUNNER_TEMP/chrome-headless-shell"
          curl --fail --location --retry 3 --output "$archive" \
            "https://storage.googleapis.com/chrome-for-testing-public/$version/$platform/chrome-headless-shell-$platform.zip"
          mkdir -p "$root"
          unzip -q "$archive" -d "$root"
          executable="$root/chrome-headless-shell-$platform/chrome-headless-shell"
          chmod +x "$executable"
          printf 'AGENT_BROWSER_EXECUTABLE_PATH=%s\n' "$executable" >> "$GITHUB_ENV"
          printf 'AGENT_BROWSER_ARGS=%s\n' '--force-prefers-no-reduced-motion' >> "$GITHUB_ENV"
          "$executable" --version

      - name: Install pinned Chrome Headless Shell on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $version = '147.0.7727.117'
          $platform = 'win64'
          $archive = Join-Path $env:RUNNER_TEMP 'chrome-headless-shell.zip'
          $root = Join-Path $env:RUNNER_TEMP 'chrome-headless-shell'
          $url = "https://storage.googleapis.com/chrome-for-testing-public/$version/$platform/chrome-headless-shell-$platform.zip"
          curl.exe --fail --location --retry 3 --output $archive $url
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Expand-Archive -LiteralPath $archive -DestinationPath $root -Force
          $executable = Join-Path $root 'chrome-headless-shell-win64\chrome-headless-shell.exe'
          "AGENT_BROWSER_EXECUTABLE_PATH=$executable" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          'AGENT_BROWSER_ARGS=--no-sandbox,--force-prefers-no-reduced-motion' | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Output "Chrome Headless Shell ${version}: $executable"
          & $executable --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run the real browser suite
        if: runner.os != 'Windows'
        shell: bash
        env:
          AGENT_BROWSER_HEADED: "1"
        run: |
          export A3S_TEST_AGENT_BROWSER="$(command -v agent-browser)"
          bash .github/scripts/run-cargo-test.sh \
            cargo test -p a3s-test-cli --test web_e2e \
            real_agent_browser_runs_the_hermetic_web_suite \
            --locked -- --ignored --exact --nocapture
          bash .github/scripts/run-cargo-test.sh \
            cargo test -p a3s-test-cli --test web_e2e \
            real_agent_browser_runs_the_embedded_testkit_suite \
            --locked -- --ignored --exact --nocapture
          bash .github/scripts/run-cargo-test.sh \
            cargo test -p a3s-test-cli --test web_e2e \
            real_agent_browser_runs_the_website_testkit_suite \
            --locked -- --ignored --exact --nocapture

      - name: Run the real browser suite on Windows
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          AGENT_BROWSER_HEADED: "1"
        run: |
          $npmRoot = (& npm root --global).Trim()
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $architecture = if ([System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture -eq [System.Runtime.InteropServices.Architecture]::Arm64) { 'arm64' } else { 'x64' }
          $browser = Join-Path $npmRoot "agent-browser\bin\agent-browser-win32-$architecture.exe"
          if (!(Test-Path -LiteralPath $browser -PathType Leaf)) {
            throw "Installed standalone browser executable not found: $browser"
          }
          $env:A3S_TEST_AGENT_BROWSER = $browser
          cargo test -p a3s-test-cli --test web_e2e `
            real_agent_browser_runs_the_hermetic_web_suite `
            --locked -- --ignored --exact --nocapture
          cargo test -p a3s-test-cli --test web_e2e `
            real_agent_browser_runs_the_embedded_testkit_suite `
            --locked -- --ignored --exact --nocapture
          cargo test -p a3s-test-cli --test web_e2e `
            real_agent_browser_runs_the_website_testkit_suite `
            --locked -- --ignored --exact --nocapture

      - name: Assert Unix browser cleanup
        if: always() && runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          runtime_roots=(/tmp)
          if [[ -n "${TMPDIR:-}" && "$TMPDIR" != /tmp ]]; then
            runtime_roots+=("$TMPDIR")
          fi

          find_survivors() {
            ps -axo pid=,stat=,command= | awk '
              $2 ~ /^Z/ || $3 ~ /(^|\/)awk$/ { next }
              /agent-browser/ { print; next }
              /Google Chrome/ && /agent-browser-chrome-/ { print; next }
              /a3s-test-browser-watchdog/ { print }
            '
          }
          find_runtimes() {
            for runtime_root in "${runtime_roots[@]}"; do
              find "$runtime_root" -maxdepth 1 -type d \
                \( -name 'a3st-*' -o -name 'a3s-test-browser-*' \) -print
            done
          }

          for _ in {1..40}; do
            survivors="$(find_survivors)"
            runtimes="$(find_runtimes)"
            if [[ -z "$survivors" && -z "$runtimes" ]]; then
              exit 0
            fi
            sleep 0.25
          done
          printf 'Surviving browser processes:\n%s\n' "$survivors" >&2
          printf 'Surviving private runtimes:\n%s\n' "$runtimes" >&2
          exit 1

      - name: Assert Windows browser cleanup
        if: always() && runner.os == 'Windows'
        shell: pwsh
        run: |
          $runtimeRoot = [System.IO.Path]::GetTempPath()
          for ($attempt = 0; $attempt -lt 40; $attempt++) {
            $survivors = @(
              Get-CimInstance Win32_Process | Where-Object {
                $_.Name -eq 'agent-browser-win32-x64.exe' -or
                ($_.Name -in @('chrome.exe', 'msedge.exe', 'chrome-headless-shell.exe') -and
                  $_.CommandLine -match 'agent-browser-chrome-')
              }
            )
            $runtimes = @(
              Get-ChildItem -LiteralPath $runtimeRoot -Directory -ErrorAction SilentlyContinue |
                Where-Object {
                  $_.Name -like 'a3st-*' -or
                  $_.Name -like 'a3s-test-browser-*'
                }
            )
            if ($survivors.Count -eq 0 -and $runtimes.Count -eq 0) {
              exit 0
            }
            Start-Sleep -Milliseconds 250
          }
          $survivors |
            Select-Object ProcessId, ParentProcessId, Name, CommandLine |
            Format-List | Out-String | Write-Error
          $runtimes | Select-Object FullName | Format-List | Out-String | Write-Error
          exit 1

  unix-installers:
    name: Installer (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-14
          - ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test Unix installer
        run: bash tests/install-unix.sh

  windows-installer:
    name: Installer (windows)
    runs-on: windows-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test PowerShell installer
        shell: pwsh
        run: ./tests/install-windows.ps1
